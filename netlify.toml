[build]
  # This replaces your --dir=public flag
  publish = "public"
  
  # This replaces your --functions flag
  functions = "netlify/functions"

[dev]
  # Optional, but helpful: Tells the local dev server what port to use
  port = 8888
